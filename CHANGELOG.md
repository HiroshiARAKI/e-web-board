# Changelog

このプロジェクトのすべての注目すべき変更はこのファイルに記録されます。

---

## [1.4.0] - 2026-04-28

### 新機能

- **PostgreSQL への移行対応** (#92)
  - 永続 DB を SQLite から PostgreSQL へ移行
  - Docker / migration / ドキュメントを PostgreSQL 前提に更新

- **S3 互換メディアストレージ対応** (#93)
  - ローカル保存に加えて S3 互換ストレージを選択可能
  - RustFS などの外部ストレージと `/uploads/...` の互換 URL を維持

- **Owner / Shared の所有スコープを導入** (#101)
  - ボード、メディア、設定、メッセージを Owner 単位で分離
  - Shared ユーザー管理と public read endpoint を追加

- **Owner サインアップにメール認証フローを追加** (#105)
  - `/signup` → メールリンク → パスワード登録 → `/pin/setup` の 2 段階フローを追加
  - 仮登録 token と再送フローを導入

- **ボードの公開設定と表示認証を追加** (#106)
  - ボードに `public` / `private` を追加し、既定値を `private` に変更
  - private ボードでは表示 URL / 公開 API / SSE で認証を要求

- **Owner 退会フローを追加** (#120)
  - 設定画面に Dangerous settings と退会導線を追加
  - メールリンクで退会を確定し、Owner 配下の不揮発データを削除

### セキュリティ・認証改善

- **PIN 要求条件を端末単位の認証キャッシュに変更** (#108)
  - `device-auth` grant ベースでフル認証有効期限を判定
  - 他ユーザーの認証状態に引きずられない PIN 導線へ変更

- **PIN リセット導線の未認証乗っ取りを防止** (#109)
  - reset URL と個人情報の未認証露出を抑止
  - `APP_PUBLIC_ORIGIN` を基準に PIN 初期化リンクを生成

- **ログイン試行制限の IP 判定を trusted proxy 前提に修正** (#110)
  - `TRUST_PROXY_HEADERS` 有効時のみ proxy header を信用
  - client + subject 単位の rate-limit bucket へ変更

- **ダッシュボード変更 API にサーバー側認可を追加** (#111)
  - settings / global media / network などの管理 API を admin 限定化

- **PIN ハッシュを scrypt に移行** (#112)
  - 固定 SHA-256 からメモリハード KDF へ移行
  - 既存 PIN は成功認証時に段階的に再ハッシュ

- **`/api/network` を管理者限定に変更** (#113)

- **production では認証 Cookie に Secure を付与** (#114)

- **Owner signup の登録 URL 生成と token 露出を harden** (#116)
  - signup URL を `APP_PUBLIC_ORIGIN` 基準に統一
  - preview URL の返却をローカル開発時に限定

### 不具合修正

- **画像スライドショーの初回停止を修正** (#123)
  - キャッシュ済み画像でも loaded 判定されるよう修正
  - 画像エラー時にもスライドショー全体が停止しないよう改善

### その他

- システムドキュメントを `docs/` 配下へ再編成 (#88)

## [1.3.0] - 2026-04-18

### 新機能

- **メールアドレス + パスワードによる管理者認証を追加** (#71)
  - PIN 認証に加えてメールアドレス + パスワードでログイン可能
  - パスワードの初期設定・変更 API を追加
  - フル認証の有効期限チェックを導入

- **複数アカウント管理を追加** (#73)
  - ユーザー管理画面を追加し、管理者 / 一般ユーザーを作成・更新・削除可能に
  - ユーザーごとのテーマ設定を保持

- **管理画面のレスポンシブ対応を強化** (#76)
  - モバイル向けサイドバーとヘッダーを追加
  - 設定画面・編集画面のレイアウトを狭い画面でも操作しやすい構成に調整

### 表示品質改善

- **スライドショー画像の先読みを追加** (#80)
  - 次に表示する画像を事前デコードし、切り替え時の待ちを削減

- **ボードのスライド間隔を画像表示時間へ同期** (#79)
  - ボード既定のスライド間隔と各画像の表示時間の整合性を改善

### 不具合修正

- **ボード編集画面のモバイル横スクロールを修正** (#82、closes #81)
  - 設定フォームのカラム縮退を見直し、スマートフォン幅でのはみ出しを防止

- **管理画面リロード時のダークモードちらつきを修正** (#84、closes #83)
  - 明示的なダークテーマに加え、システム準拠 + ダーク環境でも初回描画から正しいテーマを適用

### その他

- GitHub リリース参照先を `HiroshiARAKI/Keinage` に更新

---

## [1.2.0] - 2026-04-15

### 新機能

- **管理者 PIN 認証を追加** (#56、closes #53)
  - ダッシュボード全体を 6 桁 PIN で保護
  - 初回起動時の PIN 設定フロー（PIN 入力 → 確認 → メールアドレス登録）
  - セッション Cookie (24h TTL) によるログイン維持
  - IP ベースのレート制限（5 回失敗 / 24h でロック）
  - メール / 画面表示によるリセットリンク発行

- **呼び出し番号テンプレートを追加** (#54、closes #52/#53)
  - 発番号を大きく表示する番号呼び出し専用テンプレート
  - 番号サイズ・色・フォント・アニメーションをカスタマイズ可能

- **管理画面にダークモードを追加** (#63、closes #58)
  - システム準拠 / ライト / ダークの 3 択
  - `localStorage` で設定を不揮発保存
  - `prefers-color-scheme` 変更にリアルタイム追従
  - テーマ切替時の 300ms スムーストランジション

- **アプリ名を Keinage にリブランディング** (#67、closes #60)
  - アプリ全体のブランド名を「e-Web Board」→「Keinage」に変更
  - ランディングページにロゴ・スクロールアニメーション付きヘッダーを追加
  - LP に Inter フォントを適用
  - ファビコンを keinage.png に更新

### テンプレート・UI 拡張

- **全テンプレートにフォント選択を追加** (#50、closes #48)
  - 日本語対応フォント（DotGothic16、衡山毛筆フォント等）と英語フォント（Source Code Pro 等）を選択可能

- **シンプル掲示板ティッカーにフォントサイズ・縦位置オプションを追加** (#51、closes #49)

- **メディア表示に objectFit (cover / contain) 設定を追加** (#47、closes #46)

### 不具合修正

- **Docker build 時のプリレンダリングエラーを修正** (#62、closes #59)
  - ダッシュボード layout で `cookies()` を DB クエリより先に呼び出し
  - マイグレーション前でも `next build` が成功するように

### その他

- GitHub Issue テンプレートを追加 (#66、closes #65)
- AI コーディングエージェント向け指示ファイルを追加 (#61、closes #55)

---

## [1.1.1] - 2026-04-12

### 不具合修正

- **Docker ビルド時の SQLITE_BUSY エラーを修正** (#42)
  - DB 接続を遅延初期化 (Proxy) に変更し、Next.js ビルドワーカーの同時アクセスによるロック競合を解消
  - `busy_timeout` プラグマを追加
  - Dockerfile から存在しない `public/` ディレクトリの COPY を削除

### その他

- ランディングページ (`lp/`) を追加
- SVG ファビコンに変更

---

## [1.1.0] - 2026-04-12

### 新機能

- **全テンプレートに時刻・天気表示オプションを追加** (#29)
  - シンプル掲示板: スライドショー右上にオーバーレイ表示
  - メッセージ掲示板: ヘッダーに時計、フッター上部に天気バー
  - レトロ掲示板: ヘッダー下に時計・天気バーを表示
  - 各テンプレートの設定エディタに showClock / showWeather トグルを追加（デフォルト OFF）

- **画像リサイズ・サムネイル自動生成** (#31)
  - アップロード時に画像の長辺を最大ピクセル数でリサイズ（デフォルト 3840px）
  - 600px サムネイルを自動生成し、ダッシュボードで使用
  - 管理者設定画面から最大値の変更が可能

- **メディアの個別削除機能** (#32)
  - 設定画面にアップロード済みメディア一覧をサムネイル付きで表示
  - ディスクベースのファイル管理（孤立ファイルの検出・削除にも対応）
  - 削除時にボード紐付け情報の警告ダイアログを表示

- **管理者設定画面にSWバージョン情報を表示** (#34)
  - 現在のバージョンをリリースノートへのリンク付きで表示
  - GitHub Releases API で最新バージョンを確認し、更新がある場合に通知

- **ボード表示画面に全画面表示・マウスカーソル自動非表示機能** (#37)
  - マウスカーソルを3秒操作なしで非表示にし、動かすと再表示
  - 左下に全画面表示 / 元に戻すボタンをフロート表示
  - Fullscreen API でアドレスバー・タブ等を非表示に

---

## [1.0.0] - 2026-04-12

### 初回リリース 🎉

Web 上で動作するカスタマイズ可能なデジタルサイネージ「Keinage」の初回リリースです。

### テンプレート

- **シンプル掲示板** — メディアスライドショー＋下部テキストティッカー
- **フォトクロック掲示板** — 全画面スライドショー＋日時・天気オーバーレイ
- **レトロ掲示板** — ドットマトリクス風デザイン、文字フリップアニメーション
- **メッセージ掲示板** — API 連携によるリアルタイムメッセージ表示

### ダッシュボード (管理画面)

- ボードの作成・編集・削除
- テンプレート選択およびテンプレート別設定
- メディア (画像・動画) のドラッグ＆ドロップアップロード
- メッセージの作成・編集・削除 (優先度・有効期限設定)
- 天気予報の対象地域設定

### API (外部連携)

- ボード CRUD (`/api/boards`)
- メディア管理 (`/api/media`)
- メッセージ送受信 (`/api/messages`) — 外部システムからの呼び出しに対応
- 設定取得・更新 (`/api/settings`)
- 天気情報取得 (`/api/weather`)

### リアルタイム通信

- Server-Sent Events (SSE) によるボード表示のリアルタイム更新
- ボード・メディア・メッセージの変更即時反映
- 自動再接続機能

### デプロイ

- Docker Compose によるワンコマンド起動
- SQLite による外部 DB サーバー不要な構成
- データの Docker ボリューム永続化

### 技術スタック

- Next.js 15 (App Router) / TypeScript
- Tailwind CSS v4 / shadcn/ui / Framer Motion
- Drizzle ORM + SQLite
- Zod バリデーション
