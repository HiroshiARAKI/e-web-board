# Task 004: フォトクロック掲示板テンプレート

## ステータス
- [ ] 完了

## 概要
全画面で画像スライドショーを表示し、日付と時刻をオーバーレイ表示する「フォトクロック掲示板」テンプレートを実装する。

## やること
- [ ] `src/components/board/templates/PhotoClockBoard.tsx` の実装
  - 全画面: 画像スライドショー (Framer Motion、フェードまたはスライド切り替え)
  - オーバーレイ: 現在の日付・時刻をリアルタイム表示
  - 時計の表示位置 (上下左右) を config で切り替え可能に
- [ ] 時計コンポーネント `src/components/board/DateTimeClock.tsx` の実装
  - 日付フォーマット (YYYY/MM/DD (曜日))
  - 時刻フォーマット (HH:mm:ss)
  - クライアントサイドで毎秒更新
- [ ] テンプレートの設定項目の定義 (config)
  - スライドショーの切り替え間隔 (秒)
  - 時計の表示位置 (top-left / top-right / bottom-left / bottom-right)
  - 時計のフォントサイズ、色、背景の透過度
  - 24 時間制 / 12 時間制の切り替え
- [ ] テンプレートレジストリへの登録

## 完了条件
`/board/{boardId}` にアクセスして、全画面で画像がスライドし、指定した位置に現在時刻が表示される。
