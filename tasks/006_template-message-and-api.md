# Task 006: メッセージ掲示板テンプレート + 外部連携 API

## ステータス
- [ ] 完了

## 概要
外部システムから API 経由で受信したメッセージをリアルタイムに表示する「メッセージ掲示板」テンプレートと、外部連携用 REST API を実装する。

## やること

### テンプレート
- [ ] `src/components/board/templates/MessageBoard.tsx` の実装
  - メッセージのリスト表示 (優先度順)
  - 新着メッセージのアニメーション (ハイライト表示)
  - 期限切れメッセージの自動非表示
- [ ] テンプレートの設定項目の定義 (config)
  - 最大表示件数
  - フォントサイズ
  - 背景色、テキスト色
- [ ] テンプレートレジストリへの登録

### 外部連携 REST API
- [ ] `POST /api/messages` — メッセージ送信
  - Zod によるリクエストボディのバリデーション
  - boardId の存在確認
  - DB への保存
- [ ] `DELETE /api/messages/:id` — メッセージ削除
- [ ] `GET /api/boards/:id/messages` — ボードのアクティブメッセージ一覧取得
  - 期限切れメッセージを除外

## 完了条件
curl でメッセージを POST し、ボード画面にリアルタイムで表示される。DELETE でメッセージが消える。
